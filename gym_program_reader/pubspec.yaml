name: gym_program_reader
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  file_picker: ^5.3.2
  flutter:
    sdk: flutter
  flutter_picker: ^2.1.0
  google_fonts: ^5.1.0
  path_provider: ^2.0.15
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
